name: base_app
description: A new Flutter project.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  core:
    path: ../core
  common_deps:
    path: ../common_deps
  common_ui:
    path: ../common_ui
  app_chat:
    path: ../app_chat
  app_groups:
    path: ../app_groups
  app_user:
    path: ../app_user

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
